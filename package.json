{
  "name": "haven-proxy",
  "version": "0.1.0",
"type": "module",
  "description": "Talk to the Haven (EHBP) relay from OpenAI-compatible agents while end-to-end encryption stays on your machine. Ships an in-process AI SDK / OpenCode provider (src/provider.js) and a localhost HTTP proxy (src/index.js) that share one relay core.",
  "main": "src/provider.js",
  "exports": {
    ".": "./src/provider.js",
    "./proxy": "./src/index.js",
    "./relay": "./src/relay.js"
  },
  "bin": {
    "haven-proxy": "src/index.js"
  },
  "scripts": {
    "start": "node --env-file=.env src/index.js"
  },
  "engines": {
    "node": ">=20.6"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^3.0.11",
    "tinfoil": "^1.1.10"
  }
}
