{
  "name": "haven-proxy-app",
  "productName": "Haven Proxy",
  "version": "0.3.0",
  "private": true,
  "description": "Tray app that runs the Haven encrypted-inference proxy in the background",
  "type": "module",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dist": "node scripts/prepack.mjs && electron-builder"
  },
  "devDependencies": {
    "electron": "latest",
    "electron-builder": "latest",
    "esbuild": "^0.28.1",
    "haven-proxy": "file:.."
  }
}
